# Pinned to exact tested versions (white-hat: the in-panel updater runs
# `pip install -r requirements.txt` on every update, so open-ended floors
# would silently pull new majors or a compromised fresh release).
# To upgrade deliberately: bump a line, run both suites, commit.
fastapi==0.141.1
uvicorn[standard]==0.52.4
SQLAlchemy==2.0.52
PyJWT==2.13.0
jinja2==3.1.6
cryptography==50.0.0
pymysql==1.2.0
psycopg2-binary==2.9.13
qrcode==8.2
psutil==7.2.2
pydantic==2.13.4
